import React from "react";

const Form = () => {
    return (
        <div>
            <input type="text" id="fname"/>
            <button>Add Todo</button>
        </div>
    )
}

export default Form;