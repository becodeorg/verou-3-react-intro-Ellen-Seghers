const Form = () => {
    return (
        <div>
            <input type="text" placeholder="Write a new todo" />
            <br />
            <button>Add todo</button>
        </div>
    )
}

export default Form;